# 项目规则

- 全程使用中文和我沟通
- 所有提问、选项、确认都必须用中文
- 在开始执行前，先给完整计划
- 执行阶段尽量连续完成，不要为普通 Bash 命令反复中断
- 只有 git push、删除文件、读取敏感文件时才停下来问我

## 项目信息

- 项目名：UoA Event Agent（奥克兰/UoA 中国学生活动情报号）
- Python 虚拟环境在 `.venv/`
- 运行命令需要先 `cd /Users/rae/uoa-event-agent`
- CLI 入口：`python -m src.cli`
- AI 评分需要 `.env` 文件中的 `ANTHROPIC_API_KEY`
